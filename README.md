# Simple MCP CI/CD Server

An MCP server for GitHub Actions failure diagnosis and auto-repair.

## Quick Start

### 1. Setup Python Environment
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt